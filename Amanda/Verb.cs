namespace Amanda
{
    /// <summary>
    /// An enum which contains all supported HTTP Verbs (Methods) for accessing API methods
    /// </summary>
    public enum Verb
    {
        Get,
        Post
    }
}
